*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Suite Setup
Force Tags        QUERY    unix
Test Template     Query Template
Resource          ../../resources/keywords.txt

*** Test Cases ***    PACKAGENAME    CATALOG        RETURN CODE    RETURN STATUS                   RETURN REPORT
Query Package Name Only
                      SMCnano        application    0              ${ENDED_OK - ended normally}    SMCnano

Query Package Name Not Exist
                      NotExist       application    0              ${ENDED_OK - ended normally}    ERROR: information for "NotExist" was not found

*** Keywords ***
Query Template
    [Arguments]    ${name}    ${catalog}    ${returnCode}    ${returnStatus}    ${returnReport}
    Init Action    ${ACTION_INSTALL}
    Action Set    ${PRT_INSTALL_PKG_NAME}    SMCnano
    Action Set    ${PRT_INSTALL_PKG_SRC}    ${_WORKING_DIR}/nano-2.0.9-sol10-sparc-local
    Action Execute
    Init Action    ${ACTION_QUERY}
    Action Set    ${PRT_QUERY_PKG_NAME}    ${name}
    Action Set    ${PRT_QUERY_FILTER_BY_CAT}    ${catalog}
    Action Set    ${PRT_QUERY_FILTER_BY_ARCH}    sparc
    Action Set    ${PRT_QUERY_SEARCH_FILTER}    all
    Action Set    ${PRT_QUERY_OUTPUT_FORMAT}    package abbreviation, package name, architecture, version
    Action Set Timeout    1800
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
    Action Report Should Contain    ${returnReport}
